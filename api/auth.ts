// This file is a placeholder to overwrite a ghost file causing build errors.
// It should be safe to delete later.
export default (req: any, res: any) => {
    res.status(200).json({ message: "Diagnostics placeholder" });
};
